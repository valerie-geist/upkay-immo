<!DOCTYPE html>
<html lang="fr_FR">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Upkay{% endblock %}</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <link href="https://fonts.googleapis.com/css?family=Open+Sans:300,400,400i,600,700|Poppins:200,300,400,500,600,700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Roboto:500&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="{{ asset('styles/base-style.css') }}">
    <style>

        .encart-form{
            margin: auto;
            border: #ffc200 2px solid;
        }

        h1{
            text-align: center;
        }


        /* Taille label formulaire */
        .formulaire{
            font-size: 0.8em;
            font-family: 'Poppins', sans-serif;
            font-weight: 300;
        }

        /* Alignement texte droite*/
        .centre{
            text-align: center;
            font-style: italic;
            font-size: 0.9em;
        }

        em{
            font-weight: bold;
        }

    </style>
    {% block stylesheets %}{% endblock %}
</head>
<body>

<div class="row">
    <div class="col">
        {{ include('dashboard-components/header.html.twig') }}
    </div>
</div>
{{ include('home.html.twig') }}

<div class="container mt-5">
    {% if app.user %}
        <div class="mb-3 centre">
            Vous êtes actuellement connectés en tant que <em>{{ app.user.nomEntreprise }}</em>
        </div>
    {% endif %}
{#    <div class="bienvenue">Bienvenue sur</div>#}
    <div class="row justify-content-center">
        <div class="col-4 mt-2">
            <img src="{{ asset('images/menus/logo-upkay-immo.svg') }}" width="100%" alt="logo upkay immo">
        </div>
    </div>
    <div class="row justify-content-center">
        <div class="col-5">
            <div class="mt-5">
                <form method="post">
                    {% if error %}
                        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                    {% endif %}

                    <div class="encart-form p-4">
                        <h1>Connexion au compte</h1>
                        <div class="formulaire mt-4">
                            <div class="row mt-3">
                                <div class="col-sm col-md col-lg">
                                    <label for="inputEmail">Email : *</label>
                                    <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control form-control-sm" required autofocus>
                                </div>
                            </div>
                            <div class="row mt-4">
                                <div class="col-sm col-md col-lg">
                                    <label for="inputPassword">Mot de passe : *</label>
                                    <input type="password" name="password" id="inputPassword" class="form-control form-control-sm" required>
                                </div>
                            </div>

                            <input type="hidden" name="_csrf_token"
                                   value="{{ csrf_token('authenticate') }}"
                            >
                            <div class="form-check mt-3">
                                <input class="form-check-input" type="checkbox" name="_remember_me" id="rememberMe">
                                <label class="form-check-label" for="rememberMe">
                                    Se souvenir de moi
                                </label>
                            </div>

                            <div class="row mt-4 justify-content-center">
                                <div class="col-sm-6 col-md-6 col-lg-6">
                                    <button class="btn bouton-formulaire" type="submit" >Se connecter</button>
                                </div>
                            </div>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js" integrity="sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo" crossorigin="anonymous"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js" integrity="sha384-wfSDF2E50Y2D1uUdj0O3uMBJnjuUD4Ih7YwaYd1iqfktj0Uod8GCExl3Og8ifwB6" crossorigin="anonymous"></script>

</body>
</html>